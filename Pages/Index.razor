@page "/"
@page "/Home"
@page "/Index"

<h1>Hello, world!</h1>

Welcome to your new app!
@{ 
    for (int i = 0; i < 12; i++) { 
        <div class="row">
   @for (int j = 0; j < 12; j++) {
       Tuple<int, int> newTuple = new Tuple<int, int>(i, j);
       Tuples.Add(newTuple);
            <Cell @ref="CellComponents[newTuple]" />
            }
        </div>
    }
}
<SurveyPrompt Title="How is Blazor working for you?" />
